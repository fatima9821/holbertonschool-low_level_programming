#include "main.h"
#include <stdio.h>
/**
 * add-add two integers
 * @a: first numbr
 * @b: second numbr
 * Return: the addition of two nums
 */
int add(int a, int b);
{
	int add = a + b;

	return (add);
}
